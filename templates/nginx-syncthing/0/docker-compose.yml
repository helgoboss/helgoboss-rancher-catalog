nginx:
  image: nginx:latest
  volumes:
    - /mnt/syncthing/nginx/data:/etc/nginx/data
    - /mnt/syncthing/nginx/nginx.conf:/etc/nginx/nginx.conf
  restart: always
  ports:
    - "80:80"
    - "443:443"
  labels:
    io.rancher.container.pull_image: always